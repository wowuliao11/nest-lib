{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^8.1.8"
  },
  "scripts": {
    "publish": "lerna run build && git commit -am 'lerna build before publish' &&lerna publish from-package --yes"
  }
}